# English UI strings, grouped into one table per screen.
# (Sectioned form avoids TOML dotted-key table-reopening ambiguity.)

[app]
next = "Next"
back = "Back"
search = "Search"

[nav]
hint = "↑/↓ move · Enter select/next · Esc or Shift+Tab back · q quit"

[lang]
title = "Choose your language"
hint = "Select the language for the installer and your system"
en = "English"
uk = "Ukrainian (Українська)"

[tz]
title = "Select your time zone"
hint = "Type to filter the list"
footer = "Type to filter · ↑/↓ move · Enter confirm · Esc back"

[wifi]
title = "Network connection"
intro = "Connect to the internet, or skip if you use wired Ethernet"
scan = "Scan for networks"
adapter_pick = "Select the Wi-Fi adapter to use"
no_adapter = "No Wi-Fi adapters found"
skip_wired = "Skip — I have a wired Ethernet connection"
pick = "Select a network to join"
scanning = "Scanning…"
network = "Network"
password = "Network password"
footer = "↑/↓ move · Enter select · Esc back · Tab skips to next"

[kb]
title = "Keyboard layout"
hint = "Type to filter · Space to check layouts · the first checked is primary"
filter = "Filter layouts…"
footer = "type filter · ↑/↓ move · Space toggle · Enter confirm · Esc back"

[kern]
title = "Linux kernel"
hint = "Choose the kernel to install (installed during basestrap)"
footer = "↑/↓ move · Enter select · Esc back"
linux = "Linux"
linux_d = "standard kernel"
zen = "Linux Zen"
zen_d = "tuned for desktop responsiveness"
hardened = "Linux Hardened"
hardened_d = "security-focused"
lts = "Linux LTS"
lts_d = "long-term support, very stable"

[de]
title = "Desktop environment"
minimal_note = "No desktop — console only"
session_label = "Session"
session_hint = "← / → to switch"
dm_label = "Login screen"
footer = "↑/↓ move · ←/→ session · Enter: desktop → seat → login → next · Esc back"

[seat]
title = "Seat / login manager"
prompt = "Choose how sessions and device access are managed:"
elogind_rec = "recommended"
elogind_desc = "Universal: works with X11 and Wayland, integrates with PAM and polkit. Safe choice if unsure."
seatd_desc = "Minimal seat management only. Lighter, but best suited to Wayland setups."
x11_warn = "seatd + X11 runs Xorg as root (no libseat backend exists). The installer sets this up automatically and it works. Prefer rootless Xorg? Pick elogind."
footer = "↑/↓ choose · Enter confirm · Esc cancel"

[pkg]
title = "Additional packages"
gpu = "Graphics drivers"
selected = "Selected"
none_selected = "No extra packages selected"
search = "Type to search packages…"
popular = "Popular packages"
results = "Search results"
searching = "searching…"
no_results = "No packages match your search"
footer = "↑/↓ move · ←/→ section · type search · Space toggle · Enter next · Esc back"

[aur]
title = "AUR packages"
intro = "Install extra software from the Arch User Repository (built with paru)."
search = "Type to search the AUR…"
results = "AUR results"
popular = "Recommended from AUR"
none_selected = "No AUR packages selected"
footer = "↑/↓ move · type search · Space toggle · Enter next · Esc back"

[disk]
title = "Disk and partitions"
uefi_desc = "Modern firmware boot: GPT + EFI system partition. Recommended for real hardware and VMs with OVMF firmware."
bios_desc = "Legacy boot via MBR-style boot code on GPT. For old machines and default QEMU/VirtualBox VMs (SeaBIOS)."
swap_q = "Add a SWAP partition?"
fs = "Root filesystem"
fsopt_subvolumes = "Subvolumes (@, @home, @snapshots, @log, @cache)"
fsopt_compress = "Compression (zstd)"
fsopt_discard = "SSD TRIM (discard=async)"
fsopt_noatime = "noatime (don’t track access times)"
warn = "WARNING: the selected disk will be erased"
footer = "↑/↓ move · ←/→ boot mode & SWAP size · Space toggle SWAP · Enter next · Esc back"

fsopt_summary = "Filesystem options"
fsopt_none = "standard (none)"
fsopt_open_hint = "[o] configure"
fsopt_modal_title = "Filesystem options"
fsopt_modal_hint = "Space toggle · ↑↓ move · Enter/Esc close"
fsdesc_noatime = "Don't record when each file was last read (its access time, atime). Gain: skips a tiny write on every read — a little less SSD wear and slightly faster reads. Loss: programs that rely on access times (some mail clients, and backup or cache-cleanup tools that delete files by how long they've gone unused) can misbehave. Off by default: Linux already uses relatime, which avoids most atime writes, so the extra benefit here is modest."
fsdesc_subvolumes = "Create separate btrfs subvolumes: @ (system), @home (your files), @snapshots, @log, @cache. Gain: snapshot the system independently of your data — roll back in seconds after a bad update without touching /home; ready for snapper/Timeshift. @log and @cache stay out of system snapshots so they don't bloat them. Loss: a slightly more complex layout, but essentially nothing is given up. Recommended for btrfs."
fsdesc_compress = "Transparently compress data with zstd as it's written. Gain: saves disk space (often 30–50% on text, code and logs) and can even speed up reads on slow disks, since fewer bytes are read. Loss: a small CPU cost on writes; already-compressed files (video, JPEG, archives) are detected and left alone. On a daily-driver desktop this is almost always a win."
fsdesc_discard = "Tell the SSD which blocks are now free (TRIM), continuously in the background. Gain: the drive keeps its write performance over time instead of slowing as it fills. Loss: only useful on SSDs/NVMe — pointless on a spinning HDD; the async form runs in the background so it doesn't stall writes like the old synchronous discard did. An alternative is a periodic fstrim timer."
fschar_ext4 = "ext4 — the reliable, fast, mature classic; a safe pick for most systems."
fschar_btrfs = "btrfs — snapshots and instant rollback, zstd compression and subvolumes, but only once you enable them."
fschar_xfs = "xfs — high performance with large files and parallel writes; cannot be shrunk later."
fschar_f2fs = "f2fs — built for flash storage and SSDs (phones, eMMC, some NVMe)."
fschar_jfs = "jfs — lightweight with low CPU overhead, but lightly maintained (legacy)."
fschar_ext3 = "ext3 — an older journaled filesystem; prefer ext4 unless you have a reason (legacy)."
fschar_ext2 = "ext2 — no journal, fine for USB sticks but riskier on power loss (legacy)."
fsopt_cta = "Press [o] to set additional options."
fsopt_encrypt = "Encryption (LUKS)"
fsdesc_encrypt = "Encrypts the whole disk with LUKS. Gain: without the key the data is unreadable — protects you if the disk is lost or stolen. Unlocking is automatic: the key is stored on the encrypted root, so no second password at boot. Loss: a small encryption overhead; only meaningful when root is encrypted too (otherwise the key sits in the clear)."
[user]
title = "Accounts"
mode = "Account setup"
mode_sep = "User + separate root password"
mode_same = "User + root shares this password"
mode_sudo = "User only (root disabled, use sudo)"
mode_root = "Root only (no user account)"
hostname = "Hostname (machine name)"
username = "Username"
password = "Password"
confirm = "Confirm password"
root_password = "Root password"
root_confirm = "Confirm root password"
need_name = "Enter a username"
bad_name = "Username: lowercase, start with a letter, max 32"
need_pass = "Enter a password"
mismatch = "Passwords do not match"
need_root = "Enter a root password"
root_mismatch = "Root passwords do not match"
ok = "Looks good — press Enter to continue"
footer_mode = "↑/↓ fields · ←/→ change account mode · Enter next · Esc back"
footer_field = "↑/↓ fields · type to edit · Enter next · Esc back"

[opt]
title = "Install options"
intro = "A few choices about how the system is installed and behaves."
sudo = "sudo password"
sudo_pass = "Ask every time"
sudo_nopass = "Passwordless"
sudo_pass_hint = "You'll enter your password when running sudo (more secure)."
sudo_nopass_hint = "sudo runs without asking for a password (less secure)."
chaotic = "Chaotic-AUR repository"
chaotic_hint = "Binary repo of prebuilt AUR packages (by the Garuda Linux team): installs popular AUR software with pacman, no compiling. Adds its key, keyring and mirrorlist."
mirrors = "Optimize mirrors by speed"
mirrors_hint = "Rank pacman mirrors by real connection speed before installing, seeded from your timezone region. Slow/distant mirrors drop to the bottom. Adds a short ranking step but speeds up all downloads."
encrypt = "Disk encryption"
on = "On"
off = "Off"
encrypt_hint = "Encrypt the root partition with LUKS (you'll set a passphrase)."
scope = "Encryption scope"
scope_root = "root only (/boot plaintext)"
scope_full = "full (encrypt /boot too)"
scope_hint = "root-only is simpler; full also encrypts /boot (GRUB unlocks it)."
passphrase = "LUKS passphrase"
passphrase_hint = "Required to unlock the disk at every boot. Type to set."
bootloader = "Bootloader"
bootloader_hint = "GRUB (most features), rEFInd or Limine. Left/Right to change."
usbkey = "USB unlock key"
usbkey_hint = "Put a random LUKS keyfile on a flash drive: plugged in — the system unlocks by itself, removed — it asks for the passphrase. Left/Right picks the drive."
usbkey_warn = "WARNING: the selected flash drive will be COMPLETELY ERASED (FAT32, label ARTIXKEY)."
usbmode = "USB key mode"
usbmode_backup = "USB key + backup passphrase"
usbmode_only = "USB key ONLY"
usbmode_hint = "Backup mode: a random key on the stick unlocks automatically, AND your memorable passphrase is kept as a separate slot for if the stick is lost — two independent keys. Key-only needs no passphrase. Space/Left/Right switches."
usbmode_only_warn = "WARNING: no passphrase will exist at all — a huge random key is generated automatically and lives ONLY on the stick. The stick is the ONLY way to unlock this computer. Lost stick = lost data."
bootloader_warn = "Encrypted /boot needs GRUB — switch bootloader or use root-only encryption."
bootid = "UEFI boot entry name"
bootid_hint = "Name shown in the firmware boot menu (UEFI only). Type to edit."
footer = "↑/↓ move · ←/→ or Space toggle · Enter next · Esc back"

osprober = "Detect other OSes"
osprober_hint = "Runs os-prober so Windows / other systems get a GRUB boot entry (GRUB only)"
[sum]
prompt_title = "Provider choice"
prompt_hint = "A package above offers several providers. Pick one to install."
prompt_hint2 = "Enter = default · type a number + Enter · ↑/↓ PgUp/PgDn scroll the list"
prompt_auto = "auto-default in"
title = "Review and install"
confirm = "Press Enter to begin installation"
installing = "Installing…"
logs = "Installation log"
following = "following"
scrolled = "scrolled — double-tap ↓ to follow live"
review = "Your choices"
failed = "Installation failed"
done = "Installation succeeded"
footer_review = "Enter begin install · Esc back to edit"
footer_installing = "Installing · ↑↓ PgUp/PgDn scroll · End follow · please wait"
footer_failed = "Esc go back to fix · Enter retry install · ↑↓ scroll log"
footer_done = "Enter to continue"

[fin]
title = "Installation complete"
thanks = "Thank you so much for using this TUI installer for Artix!"
donate = "And please don't forget to donate to weaken russia's war machine — so more russians become good ones!"
support = "Support Ukraine's defense:"
reboot = "Continue"

[mode]
title = "What would you like to do?"
hint = "Install a fresh system, or recover an existing one."
install = "Install system"
recovery = "Recover system"

[rec]
title = "System recovery"
hint = "Mount an existing install and open a chroot shell to repair it."
root_part = "Root partition  (path · size · fs · label)"
unlock = "Unlock method"
unlock_none = "None (unencrypted)"
unlock_passphrase = "Passphrase"
unlock_usbkey = "USB key"
passphrase = "LUKS passphrase"
passphrase_na = "(not needed for the selected method)"
status = "Status"
status_idle = "Select the root partition and unlock method, then choose Mount."
mount = "Mount & detect"
open_shell = "Open root chroot shell"
back_to_mode = "Esc back"
footer = "↑/↓ select · Enter next · Esc back"
footer_mounted = "Enter opens a root chroot · type 'exit' there to unmount · Esc back"

[storage]
title = "Additional disks"
intro1 = "Assign other disks and partitions a mountpoint (optional)."
intro2 = "Empty disks are formatted & mounted; existing partitions (e.g. Windows NTFS) are mounted keeping their data."
none = "No additional disks or partitions detected."
skip = "skip"
keep = "keep data"
fmt_tag = "format"
mnt_tag = "existing"
ntfs_warn = "NTFS mounts read-write — disable Windows Fast Startup/hibernation first to avoid corruption."
enc_warn = "Root isn't encrypted (the «Bootloader & encryption» step). Without it this disk's key is stored in the clear, so the encryption barely helps. Go back to encrypt the system too."
footer = "↑↓ row · ←→ change value · o options · type a folder name · Enter next"

intro3 = "Existing systems (e.g. Windows/NTFS) are detected automatically. Each disk below explains what happens to it; ←→ mountpoint, a/d filesystem."
desc_fmt_off = "Not used — this disk is left untouched. Press ←→ to choose where to attach it (it will then be formatted); a/d picks the filesystem."
desc_mnt_off = "Existing %FS% partition — not attached right now. Press ←→ to make it available in the system (its data is preserved, never formatted)."
mean_home = "Becomes your home folder (/home) — your documents, downloads, desktop, photos and app settings will live here, kept separate from the system."
mean_data = "Becomes a separate storage area at /data for your own files — videos, backups, collections. The system leaves it alone; you put files there yourself."
mean_srv = "Becomes a server-data folder at /srv — websites, shared folders, databases."
mean_storage = "Becomes extra storage at /mnt/storage — a separate place outside the system where you can keep any files; it simply appears as that folder."
mean_windows = "Your Windows files become available at %MP% — you'll be able to open and copy them straight from Linux."
mean_media = "Becomes media storage at /mnt/media — movies, music, photos; handy for a shared media library."
mean_generic = "Will be mounted at the %MP% folder."
act_format = "The disk will be COMPLETELY ERASED and formatted as %FS%."
act_keep = "The %FS% partition is not formatted — its existing data is kept."
act_reformat = "The partition (%OLD%) will be REFORMATTED as %FS% — all data currently on it is erased."
custom_title = "Custom folder"
custom_base = "Base"
custom_base_home = "Home folder"
custom_base_mnt = "/mnt"
custom_name = "Name"
custom_preview = "Path:"
custom_hint = "← / → base · type a name · Enter confirm · Esc cancel"
mean_custom = "Custom folder “%NAME%” — %WHERE%."
mean_custom_home = "in your home, shown right away in the file manager"
mean_custom_mnt = "under /mnt, shown in the file manager sidebar via a bookmark"
lbl_format = "Format:"
lbl_mount = "Mount:"
btn_opts = "options"
btn_name = "name"
base_home = "home folder"
base_custom = "custom path"
custom_path = "Path:"
lbl_encrypt = "Encryption:"
enc_on = "on"
enc_off = "off"

[sec]
title = "Bootloader & encryption"
intro = "Choose the system bootloader and set up disk encryption."